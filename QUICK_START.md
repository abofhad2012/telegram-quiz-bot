# 🚀 دليل البدء السريع

اتبع هذه الخطوات البسيطة لتشغيل بوت الاختبارات على تلقرام في دقائق!

---

## الخطوة 1️⃣: إنشاء البوت على تلقرام

1. افتح تطبيق **تلقرام**.
2. ابحث عن **`@BotFather`** وابدأ محادثة معه.
3. اكتب الأمر `/newbot`.
4. اختر **اسماً** للبوت (مثل: "بوت اختبارات الشبكات").
5. اختر **اسم مستخدم** فريداً ينتهي بـ `bot` (مثل: `network_quiz_bot`).
6. **احفظ التوكن** الذي سيرسله لك BotFather (مثال: `1234567890:ABC-DEF...`).

---

## الخطوة 2️⃣: تثبيت المكتبات

افتح الطرفية (Terminal) واكتب:

```bash
pip install python-telegram-bot==20.7
```

---

## الخطوة 3️⃣: تعديل الكود

1. افتح ملف `bot.py` في محرر النصوص المفضل لديك.
2. ابحث عن السطر:
   ```python
   TOKEN = "YOUR_BOT_TOKEN_HERE"
   ```
3. استبدله بالتوكن الخاص بك:
   ```python
   TOKEN = "1234567890:ABC-DEF..."
   ```
4. **احفظ الملف**.

---

## الخطوة 4️⃣: تشغيل البوت

في الطرفية، انتقل إلى مجلد البوت وشغّله:

```bash
cd telegram_bot
python3 bot.py
```

سترى رسالة: `🤖 البوت يعمل الآن...`

---

## الخطوة 5️⃣: اختبر البوت!

1. افتح تلقرام.
2. ابحث عن اسم المستخدم الذي اخترته للبوت.
3. اكتب `/start` لبدء المحادثة.
4. اكتب `/quiz` لبدء الاختبار!

---

## ✅ تم! البوت يعمل الآن

**الأوامر الأساسية:**
- `/quiz` - بدء اختبار جديد
- `/score` - عرض نتيجتك
- `/help` - المساعدة

**استمتع بالاختبار! 🎓**

---

## ⚠️ ملاحظات مهمة

- **لا تشارك التوكن** مع أي شخص!
- لإيقاف البوت، اضغط `Ctrl + C` في الطرفية.
- لإضافة أسئلة جديدة، عدّل ملف `questions.json`.

---

للمزيد من التفاصيل، راجع ملف `README.md` الكامل.
