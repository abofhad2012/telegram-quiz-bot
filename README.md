# بوت اختبارات أساسيات شبكات الحاسب

هذا المشروع يحتوي على بوت تلقرام تفاعلي لاختبار معلوماتك في أساسيات شبكات الحاسب. البوت يقدم أسئلة اختيار من متعدد ويقوم بتتبع نتيجتك.

## 🌟 الميزات

- **اختبارات تفاعلية:** أسئلة اختيار من متعدد مع أزرار.
- **تغذية راجعة فورية:** يعرض الإجابة الصحيحة والشرح بعد كل سؤال.
- **تتبع النقاط:** يحسب نتيجتك ونسبتك المئوية.
- **إحصائيات مفصلة:** يعرض تقييماً لأدائك.
- **أسئلة عشوائية:** يختار الأسئلة بشكل عشوائي من قاعدة البيانات.
- **سهولة التخصيص:** يمكنك بسهولة إضافة أو تعديل الأسئلة في ملف `questions.json`.

---

## 📋 المتطلبات

- Python 3.7 أو أحدث
- `pip` لإدارة الحزم

---

## 🚀 خطوات الإعداد والتشغيل

اتبع هذه الخطوات لتشغيل البوت على جهازك الخاص.

### 1. الحصول على توكن البوت (Bot Token)

أولاً، ستحتاج إلى الحصول على توكن (Token) من تلقرام لإنشاء البوت الخاص بك.

1. افتح تطبيق تلقرام وابحث عن **`@BotFather`**.
2. ابدأ محادثة معه واكتب الأمر `/newbot`.
3. اتبع التعليمات التي يطلبها منك، مثل اختيار اسم للبوت واسم مستخدم.
4. بعد الانتهاء، سيمنحك `BotFather` توكن فريداً. **احتفظ بهذا التوكن جيداً فهو سري!**

   > مثال على التوكن: `1234567890:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`

### 2. تثبيت المكتبات المطلوبة

قم بتثبيت المكتبات اللازمة لتشغيل البوت باستخدام `pip`.

```bash
# تثبيت مكتبة python-telegram-bot
pip install -r requirements.txt
```

### 3. إعداد التوكن في الكود

افتح ملف `bot.py` وابحث عن السطر التالي:

```python
TOKEN = "YOUR_BOT_TOKEN_HERE"
```

استبدل `"YOUR_BOT_TOKEN_HERE"` بالتوكن الذي حصلت عليه من `BotFather`.

```python
# مثال:
TOKEN = "1234567890:ABC-DEF1234ghIkl-zyx57W2v1u123ew11"
```

### 4. تشغيل البوت

الآن، يمكنك تشغيل البوت باستخدام الأمر التالي في الطرفية (Terminal):

```bash
python3 bot.py
```

إذا تم كل شيء بشكل صحيح، سترى رسالة في الطرفية تفيد بأن البوت يعمل الآن:

```
INFO:__main__:🤖 البوت يعمل الآن...
```

الآن يمكنك فتح تلقرام والبحث عن اسم المستخدم الخاص ببوتك لبدء الاختبار!

---

## 🤖 أوامر البوت

- `/start` - بدء محادثة جديدة وإعادة تعيين النقاط.
- `/quiz` - بدء اختبار جديد أو الحصول على سؤال جديد.
- `/score` - عرض نتيجتك الحالية وتقييمك.
- `/stats` - عرض إحصائيات مفصلة عن أدائك.
- `/help` - عرض رسالة المساعدة.
- `/cancel` - إلغاء السؤال الحالي.

---

## 🔧 تخصيص الأسئلة

يمكنك بسهولة تعديل أو إضافة أسئلة جديدة.

1. افتح ملف `questions.json`.
2. اتبع نفس التنسيق لإضافة سؤال جديد:

```json
{
  "id": 21, // رقم فريد للسؤال
  "question": "ما هو السؤال الجديد؟",
  "options": [
    "خيار 1",
    "خيار 2",
    "خيار 3",
    "خيار 4"
  ],
  "correct_answer": 0, // فهرس الإجابة الصحيحة (يبدأ من 0)
  "explanation": "شرح مفصل للإجابة الصحيحة."
}
```

3. تأكد من وضع فاصلة (`,`) بعد كل سؤال ما عدا الأخير.
4. أعد تشغيل البوت لتطبيق التغييرات.


---

## 📁 هيكل المشروع

```
telegram_bot/
│
├── bot.py                  # الكود الرئيسي للبوت
├── questions.json          # قاعدة بيانات الأسئلة
├── requirements.txt        # المكتبات المطلوبة
└── README.md              # ملف التعليمات (هذا الملف)
```

---

## 💡 نصائح وملاحظات

### الأمان

- **لا تشارك التوكن الخاص بك مع أي شخص!** التوكن يمنح التحكم الكامل في البوت.
- إذا تم تسريب التوكن، يمكنك إنشاء توكن جديد من `@BotFather` باستخدام الأمر `/token`.

### التشغيل المستمر

إذا أردت أن يعمل البوت بشكل مستمر دون الحاجة لإبقاء الطرفية مفتوحة، يمكنك استخدام أحد الحلول التالية:

#### 1. استخدام `screen` أو `tmux` على Linux/macOS

```bash
# تثبيت screen
sudo apt install screen  # على Ubuntu/Debian

# إنشاء جلسة جديدة
screen -S telegram_bot

# تشغيل البوت
python3 bot.py

# للخروج من الجلسة مع إبقاء البوت يعمل، اضغط:
# Ctrl + A ثم D

# للعودة إلى الجلسة لاحقاً:
screen -r telegram_bot
```

#### 2. استخدام خدمة استضافة مجانية

يمكنك استضافة البوت على منصات مجانية مثل:

- **Heroku** (مع قيود على الاستخدام المجاني)
- **PythonAnywhere** (خطة مجانية محدودة)
- **Replit** (مناسبة للمشاريع الصغيرة)
- **Railway** (خطة مجانية محدودة)

### إضافة ميزات إضافية

يمكنك تطوير البوت بإضافة ميزات مثل:

- **قاعدة بيانات دائمة:** استخدام SQLite أو PostgreSQL لحفظ بيانات المستخدمين بشكل دائم.
- **لوحة متصدرين:** عرض أفضل النتائج بين المستخدمين.
- **فئات مختلفة:** تقسيم الأسئلة إلى فئات (طبقة النقل، طبقة الشبكة، إلخ).
- **توقيت الأسئلة:** إضافة مؤقت لكل سؤال.
- **شهادات:** إرسال شهادة إنجاز عند إكمال عدد معين من الأسئلة.

---

## 🐛 حل المشاكل الشائعة

### المشكلة: البوت لا يستجيب

**الحل:**
- تأكد من أن التوكن صحيح.
- تحقق من اتصالك بالإنترنت.
- تأكد من أن البوت يعمل في الطرفية ولا توجد رسائل خطأ.

### المشكلة: خطأ في استيراد المكتبات

**الحل:**
```bash
# تأكد من تثبيت المكتبات
pip install --upgrade python-telegram-bot
```

### المشكلة: الأسئلة لا تظهر

**الحل:**
- تأكد من أن ملف `questions.json` موجود في نفس المجلد مع `bot.py`.
- تحقق من صحة تنسيق JSON (يمكنك استخدام مواقع مثل jsonlint.com للتحقق).

### المشكلة: البوت يتوقف بعد إغلاق الطرفية

**الحل:**
- استخدم `screen` أو `tmux` كما هو موضح أعلاه.
- أو استضف البوت على خدمة سحابية.

---

## 📊 مثال على استخدام البوت

### 1. بدء المحادثة

المستخدم: `/start`

البوت:
```
👋 مرحباً أحمد!

🎓 بوت اختبارات أساسيات شبكات الحاسب

هذا البوت يساعدك على اختبار معلوماتك في مجال شبكات الحاسب...
```

### 2. بدء الاختبار

المستخدم: `/quiz`

البوت:
```
❓ السؤال رقم 1:

أي من البروتوكولات التالية يعمل على المنفذ رقم 443؟

[A) HTTP]  [B) FTP]  [C) HTTPS]  [D) SMTP]
```

### 3. الإجابة على السؤال

المستخدم: *يضغط على زر C) HTTPS*

البوت:
```
✅ إجابة صحيحة! 🎉

💡 الشرح:
HTTPS يعمل على المنفذ 443 ويستخدم للاتصال الآمن المشفر مع مواقع الويب.

📊 نتيجتك الحالية:
1 / 1 (100%)
```

### 4. عرض النتيجة

المستخدم: `/score`

البوت:
```
📊 نتيجتك الحالية:

✅ الإجابات الصحيحة: 8
❌ الإجابات الخاطئة: 2
📝 إجمالي الأسئلة: 10
📈 النسبة المئوية: 80%

🏆 التقييم: جيد جداً 👍
```

---

## 🤝 المساهمة والتطوير

إذا كنت ترغب في تحسين البوت أو إضافة ميزات جديدة:

1. قم بعمل Fork للمشروع.
2. أنشئ فرعاً جديداً للميزة الخاصة بك.
3. قم بإجراء التعديلات والاختبار.
4. أرسل Pull Request مع شرح للتغييرات.

---

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتعليمي.

---

## 📧 الدعم

إذا واجهت أي مشاكل أو لديك اقتراحات، لا تتردد في:

- فتح Issue على GitHub.
- التواصل مع المطور.

---

## 🙏 شكر وتقدير

- **مكتبة python-telegram-bot:** لتوفير واجهة برمجية سهلة الاستخدام لتطوير بوتات تلقرام.
- **Telegram:** لتوفير منصة رائعة للبوتات.
- **جميع المساهمين:** في تطوير وتحسين هذا المشروع.

---

**بالتوفيق في اختباراتك! 🎓✨**
